# 湘南台キッチンスタジオ システム仕様 ＆ UI/UX設計書（SYSTEM_SPEC_AND_UI_DESIGN.md）

> **最終更新日**: 2026-09-26  
> **対象事業**: 湘南台キッチンスタジオ（Shonandai Kitchen Studio）  
> **管轄**: 摂取資源株式会社（代表取締役社長：岡本直樹）  
> **開発・保守**: AI（Antigravity）  
> **本ドキュメントの目的**:  
> これまでWebサイトおよび管理者ダッシュボードの開発で実施された**システムアーキテクチャ、データモデル、UI/UX設計思想、計算ロジック、および岡本代表からのフィードバック・改修履歴**を網羅し、次回以降のセッションでAIが一瞬で前提コンテキストを把握し、ブレのない迅速かつ正確な実装を行えるようにするためのマスター仕様書です。

---

## 1. プロジェクト基本情報 ＆ 環境設定

| 項目 | 内容 |
| :--- | :--- |
| **施設名称** | 湘南台キッチンスタジオ（Shonandai Kitchen Studio） |
| **所在地** | 神奈川県藤沢市湘南台4-15-5（湘南台大橋・土棚石川線至近） |
| **施設用途** | 菓子製造業許可・飲食店営業許可完備 レンタルキッチンスタジオ |
| **運営会社** | 株式会社APRECIO 様（受託・開発管理：摂取資源株式会社） |
| **営業時間・枠** | 9:00 〜 21:00（1時間単位・1日最大12スロット） |
| **公式Webサイト** | [https://onaoki0503-lgtm.github.io/shonandai-kitchen-studio/](https://onaoki0503-lgtm.github.io/shonandai-kitchen-studio/) |
| **管理者ポータル** | [https://onaoki0503-lgtm.github.io/shonandai-kitchen-studio/dashboard.html](https://onaoki0503-lgtm.github.io/shonandai-kitchen-studio/dashboard.html)<br>*(旧URL `admin.html` も完全同期)* |
| **認証パスコード** | **`shonandai2026`** |
| **GitHubリポジトリ** | [https://github.com/onaoki0503-lgtm/shonandai-kitchen-studio](https://github.com/onaoki0503-lgtm/shonandai-kitchen-studio)（`main` ブランチ） |
| **通知先メール** | `o.naoki0503@intakingresources.com`（FormSubmit連携） |

---

## 2. UI/UX 設計原則（岡本代表のフィードバックに基づく設計思想）

本プロジェクトにおけるUIデザインおよびユーザー体験は、岡本代表の明確なディレクションに基づき、徹底した**「情報の削ぎ落とし（ミニマリズム）」「モバイルファースト」「直感的な操作連動」**が基本方針となっています。今後の改修時も以下のルールを厳守してください。

### ① ミニマリズム ＆ 情報過多の徹底排除
- **冗長な表記の削除**:
  - 同一画面内に「9:00〜21:00」という営業時間表記が何箇所も重複していたのを整理し、カレンダー上部1箇所のみに集約。
  - 「空室率〇%」のような細かな数値解説や「空き枠集計バッジ」は削除。
- **装飾・SNS機能の削減**:
  - メインタイトル上の余計なバッジ、SNSシェアボタン、お気に入り（ハート）マークなどは「不要・ごちゃつく」ため排除。
- **ダッシュボード上部（ヘッダー）の簡素化**:
  - システムバージョン表記や雑多なリンクを全廃し、「ロゴ・タイトル」「担当者情報」「スタッフ管理」「Webサイト」「ログアウト」のみに絞り込む。

### ② スマートフォン最適化（Mobile-First & レスポンシブ厳守）
- **文字の縦折れ・枠外はみ出しの完全防止**:
  - 狭い端末（幅375pxなど）でヘッダーやボタンの文字が1文字ずつ改行されて縦書き状に崩れるのを防ぐため、全要素に `whitespace-nowrap flex-shrink-0` を適用。
  - スマホ幅では要素テキストを自動短縮：
    - タイトル：`Shonandai Kitchen Studio` → `Kitchen Studio`
    - 担当者情報：`[👤 岡本直樹 管理者]` → `[👤 管理者]`
    - スタッフ管理：`スタッフ管理` → `スタッフ`
    - Webサイト：`Webサイト` → `Web`
    - ログアウト：テキスト非表示・アイコンのみ表示
- **モーダルの操作性向上**:
  - モーダル下部中央に独立した「✕ 閉じる」ボタンを常設。
  - 背景（backdrop）タップや `Esc` キー押下でも即座に閉じられるようにし、スマホ画面端での誤操作や閉じにくさを解消。

### ③ 導線の一本化 ＆ 直感的な操作性
- **カレンダー日付選択パネルへの機能統合**:
  - 独立したセクション（「休業日管理テーブル」や「検索フィルターバー」）をページ中盤に乱立させず、**カレンダーの日付をクリックしたときに表示される下部パネルに集約**。
  - 「＋ この日に手動で予約を追加する」の真横に「＋ 休業日を追加する」ボタンを配置し、日付選択からワンクリックで設定可能。
  - 既に休業日の場合は「休業日（予約停止中）」バッジが表示され、ボタンが「休業日設定を解除する」に自動切り替わる。
- **アコーディオン化による省スペース化**:
  - 「運営からのご報告」と「口コミ・レビュー管理」は、普段はタイトルと公開バッジのみ表示された四角いカードとして縦並びに配置。クリック/タップで全幅展開し、邪魔にならずに必要な時だけ操作可能。
- **誤操作防止（バリデーション＆誘導）**:
  - カレンダーから日付・時間帯を選択せずに予約申し込みボタンを押した場合、モーダルは開かず「カレンダーで日時指定してから選択してください」と注釈メッセージを表示してカレンダーへ誘導。

---

## 3. システムアーキテクチャ ＆ データモデル

### ① テクノロジースタック
- **フロントエンド**: HTML5, Vanilla JavaScript (ES6+), Tailwind CSS (CDN版), Lucide Icons, Chart.js
- **状態管理・データ保存**:
  - **Primary**: Supabase（クラウドPostgreSQL）
  - **Fallback / Local Cache**: `localStorage`（オフラインやSupabase通信障害時も停止せずシームレスに動作）
- **外部通知**: FormSubmit API（顧客予約・問い合わせを管理者に即時HTMLメール送信）
- **バックアップ**: Google Apps Script (`web/gas_form_handler.js`) によるスプレッドシート連携

### ② Supabase / 内部データモデル定義

#### 1. 予約データ (`bookings`)
```typescript
interface Booking {
  id: string;                  // UUID または bk_タイムスタンプ
  guest_name: string;          // ご予約者氏名
  guest_kana: string;          // フリガナ
  guest_phone: string;         // 電話番号
  guest_email: string;         // メールアドレス
  usage_type: string;          // ご利用用途（菓子製造 / 料理教室 / 撮影 / その他）
  guest_count: number;         // ご利用人数
  reserved_date: string;       // 利用日 (YYYY-MM-DD)
  start_time: string;          // 開始時刻 (HH:00)
  end_time: string;            // 終了時刻 (HH:00)
  total_hours: number;         // 利用合計時間
  plan_name: string;           // 適用プラン名
  total_price: number;         // お支払い見込み金額 (円)
  discount_amount: number;     // パック割引等適用額 (円)
  status: 'new' | 'confirmed' | 'completed' | 'cancelled'; // ステータス
  admin_memo: string;          // 管理者専用内部メモ
  created_at: string;          // 申込日時
}
```

#### 2. 口コミ・レビューデータ (`reviews`)
```typescript
interface Review {
  id: string;
  author_name: string;         // 投稿者名（イニシャル等）
  rating: number;              // 星の数 (1 〜 5)
  usage_type: string;          // 用途
  title: string;               // タイトル
  comment: string;             // レビュー本文
  is_approved: boolean;        // 管理者承認フラグ（trueでWebサイトに公開反映）
  created_at: string;
}
```
*※ Webサイトのヘッダーに表示される星評価と総レビュー数は、承認済み（`is_approved === true`）のレビューのみから動的に平均値・件数を正確に自動算出。*

#### 3. 運営からのご報告・お知らせ (`announcements`)
```typescript
interface Announcement {
  id: string;
  date: string;                // 掲載日 (YYYY.MM.DD)
  tag: string;                 // カテゴリバッジ（お知らせ / メンテナンス等）
  content: string;             // ご報告内容
  is_published: boolean;       // 公開フラグ
  created_at: string;
}
```
*※ Webサイトトップでは料金案内ボックスの直下、またはサイドバーに写真付きリスト（縦バー・ドット日付・下線区切り）形式でスマートに掲載。*

#### 4. スタッフ・認証アカウント (`staffs`)
```typescript
interface Staff {
  id: string;
  name: string;                // 氏名（例: 岡本直樹）
  email: string;               // メールアドレス
  role: 'admin' | 'staff';     // 権限（admin: 全機能・スタッフ管理可能 / staff: 閲覧・更新）
  created_at: string;
}
```

#### 5. 休業日設定 (`holidays`)
```typescript
interface Holiday {
  date: string;                // YYYY-MM-DD
  reason: string;              // 理由（定休日 / 施設メンテナンス / 貸切等）
  created_at: string;
}
```

---

## 4. 主要計算ロジック ＆ 機能仕様

### ① 公式料金表準拠の料金計算シミュレーション
- **営業時間**: 9:00 〜 21:00（最大12時間）
- **8時間以上上限パック**:
  - 単純な時間加算ではなく、8時間を超える長時間利用時はデイタイム上限パック料金（お得な固定上限額）が自動適用。
  - 通常1時間料金の積算額と比較し、「通常料金より〇〇円OFF」の割引バッジを自動算出・表示。
- **プランカード連動**:
  - プランカードをクリックすると薄青枠でハイライトされ、時間スロット選択と即座にState同期。

### ② カレンダー空室率計算 ＆ 記号判定ロジック
- 各日付の利用可能枠（9:00〜21:00 = 12時間枠）に対して、予約済みスロット数を集計：
  - **休業日**: `×`（赤色バッジ / 「休業日」）
  - **空室率 80% 以上**: `◎`（緑色 / 余裕あり）
  - **空室率 40% 〜 79%**: `◯`（青色 / 空きあり）
  - **空室率 1% 〜 39%**: `△`（黄色 / 残りわずか）
  - **空室率 0%**: `×`（灰色 / 満室）
- **カレンダー閲覧範囲**:
  - 当月、翌月、翌々月の3ヶ月間。月送りボタンで前後の月に移動可能（過去月への移動は無効化）。

### ③ 管理ダッシュボードの分析グラフ（Chart.js）
- **PV × 予約相関グラフ**:
  - アクセス数（PV）と予約成立数の相関を視覚化。
  - **期間切り替え機能**: 「1週間」「2週間」「1ヶ月」「半年」「1年」のボタンで表示期間を即座に再集計・再描画。

---

## 5. これまでの指示 ＆ 改修履歴（Changelog）

岡本代表からの指示内容と、それに対応した実装の時系列全ログです：

| 日時 | コミット | 指示概要（ユーザーリクエスト） | 実装・改修内容 |
| :--- | :--- | :--- | :--- |
| **2026-09-26 12:55** | `e52f17f` | スマホ表示にすると崩れる。綺麗に整えて | ヘッダーのレスポンシブ最適化。スマホ時の文字縦折れを完全解消、テキスト短縮表示、`whitespace-nowrap` 徹底。 |
| **2026-09-26 12:49** | `7808d7a` | お知らせと口コミのタブを横並びから縦並びにして開くようにして | 2列グリッドを解除し、縦並びのアコーディオンカードに配置変更。クリックで全幅展開。 |
| **2026-09-26 12:41** | `19ed336` | 文字改行修正、上下余白削減、2枚目不要削除、3枚目タブ縮小して四角タップ開閉にして | 空室パネルの改行解消＆余白削減、不要な台帳フィルターバー削除、ご報告・口コミ管理を四角いアコーディオンカードに刷新。 |
| **2026-09-26 12:29** | `3070de9` | 休業日は日付選択して「手動予約追加」の横に休業日追加にして。2枚目の独立タブは削除 | 独立した休業日セクションを完全撤廃し、カレンダー日付選択パネルへ休業日追加・解除ボタンを統合。 |
| **2026-09-26 12:15** | `0617602` | ダッシュボード上部を湘南台スタジオ・スタッフ・Web・ログアウトだけに整理。グラフ期間切替追加 | ヘッダー余計なバッジ・リンク全削除、PV×予約相関グラフに期間切り替え（1週〜1年）を実装。 |
| **2026-09-26 12:00** | `9569666` | 上部タブ・ハート・シェア機能削除。レビュー表示件数と星の数を連動 | タイトル上バッジ・ハート・シェアを削除し、ヘッダーのレビュー表示を承認済みレビューと完全連動。 |
| **2026-09-26 11:45** | `09910d5` | 画面の情報量整理。9:00〜21:00表記重複削除、空室率%説明削除、空き枠集計表記削除 | カレンダー・タイムラインの情報過多を整理し、視認性を大幅向上。 |
| **2026-09-26 11:30** | `ca640ac` | 日付未選択で申し込まないよう、注釈を出してタブが開かないようにして | 日時未指定時はモーダルを開かず「カレンダーで日時指定してから選択してください」と注釈・誘導。 |
| **2026-09-26 11:15** | `9b0d59b` | タブを消すばつ印を下部中央に作成、他画面タップでも消せるようにして | モーダル下部中央に閉じるボタンを常設し、背景タップおよびEscキーで閉じられるようスマホ操作性改善。 |
| **2026-09-26 11:00** | `c2b3bfa` | ご報告は料金四角ではなく下に写真の感じで記載して | 料金ボックス直下に縦バー・ドット日付・下線区切りのスマートなリストデザインで配置。 |

---

## 6. AIアシスタントへの開発・改修行動規範（PROMPT GUIDELINES）

本プロジェクトのファイルを編集・改修する際は、必ず以下の原則に従ってください：

1. **画面の統一感とデザインルールの遵守**:
   - 原則「白黒モノトーン＋青アクセント（`#2892d8`）」のトーン＆マナーを維持すること。
   - 余計なボタン、装飾、バッジを勝手に追加しないこと（「必要最小限に整理する」のが岡本代表の方針）。
2. **スマホ表示の同時検証**:
   - 新しいUI要素を追加する際は、必ずスマホ幅（375px）で文字折れや崩れが発生しないか確認し、`whitespace-nowrap` やレスポンシブクラス（`sm:`、`md:`）を適用すること。
3. **同期ファイルの二重更新**:
   - `dashboard.html` を更新した際は、必ず `admin.html` にも同一内容を同期すること（`cp dashboard.html admin.html`）。
4. **Git・台帳記録の自動実行**:
   - 作業完了時は必ず Git コミット＆プッシュ（`BypassSandbox: true`）を行い、`inbox/decisions_log.md` および `actionlog/YYYY-MM.md` に即時記録すること。
